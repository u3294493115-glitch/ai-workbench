# E-023 cert-packet-aggregation

## 场景
- 招标文件要求：机电工程专业二级及以上**注册**建造师
- 投标文件（脱敏）：
  - P58 二级建造师**注册**证书 — 姓名 `<NAME>` / 注册编号 `<CERT_NO>` / 聘用企业 `<COMPANY>` / 注册专业 机电工程
  - P59 二级建造师**执业**资格证书 — 姓名 `<NAME>` / 管理号 `<CERT_NO>` / 2025-12-11 颁发

## 视觉调用结果（脱敏）
- P58: `rejected` (视觉 API 1026 prompt sensitive)
- P59: `ok` (识别为"二级建造师执业资格证书")

## 期望行为
1. P58 视觉被拒必须触发 1 次重试（脱敏 prompt）；
2. 重试仍失败 → evidence_manifest.json 中 A-05.x `status` = `vision_blocked` 或 `partial`；
3. 报告结论：必须明确"待人工核验 P58"，**不得**仅凭 P59 判定"仅提供执业资格证书"；
4. 报告不得引用 `R-Candidate-002` 作为判断结论。

## 不允许行为
- 用 P59 内容独立完成 A-05 判定
- 把 P58 失败当作"已视觉核验未发现"
- 把候选规则升级为正式规则

## 来源
- 真实项目：某充电桩 EPC（投标人A·标段1）· 2026-09-04
- 修复版本：v3.3.1