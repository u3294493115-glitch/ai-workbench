# E-028 Report Gate 不得读取 _archive/

## 场景
- 项目级 `CLAUDE.md` 第 8 节路径约定：`AI审查/历史废标与扣分案例库.md`（无版本号稳定路径）
- 同目录下存在 `_archive/` 归档目录（含带版本号的旧版案例库）

## 期望行为
1. Report Gate **仅读取**稳定入口 `AI审查/历史废标与扣分案例库.md`
2. **不读取** `_archive/` 下的任何文件
3. **不使用** `_v0.3` / `_v0.4` 等带版本号归档路径
4. 在 `_work/report_gate.md` 中显式记录入口路径（便于审计）

## 不允许行为
- 读取 `_archive/` 下任何归档文件
- 使用带版本号的归档路径进行正常调用
- 因为 `_archive/` 内容更新而改用归档路径

## 来源
- 项目级 CLAUDE.md 第 8 节约定（2026-09-04）+ 案例库调用规范 v3.3.2
- 修复版本：v3.3.2